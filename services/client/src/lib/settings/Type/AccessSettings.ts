export type AccessSettings = {
  allowGuestUploads: boolean;
  allowUnauthenticatedAccess: boolean;
  requireAuthForMediaShares: boolean;
  requireAuthForCollectionShares: boolean;
  requireSsl: boolean;
};
